%p#notice= notice
%h1 Orders
%table
  %thead
    %tr
      %th Ordered items
      %th Recipe ingredients
      %th{:colspan => "3"}
  %tbody
    - @orders.each do |order|
      %tr
        %td= order.ordered_items
        %td= order.recipe_ingredients
        %td= link_to 'Show', order
%br/
= form_tag orders_path do
  = submit_tag "Order groceries"
